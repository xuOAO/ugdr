# F03-S06 Implementation Record

## Source

- Reviewed feature design: [F03 Daemon 控制面与对象生命周期](../v1_docs/F03_Daemon_控制面与对象生命周期/F03_Daemon_控制面与对象生命周期_功能文档.md), Feishu revision `65`.
- Reviewed step design: [F03-S06 公开 API 集成与控制面验收](../v1_docs/F03_Daemon_控制面与对象生命周期/F03-S06_公开_API_集成与控制面验收_步骤文档.md), Feishu revision `12`, synchronized in `agent` mode with reviewed body hash `a7c862514b93fa20574fdd29de220412c9d3e5dd845f231a759d3d4186ab6fed`.
- Design handoff commit: `0b1c088` on `codex/f03-s06-public-api-control-acceptance`.
- Draft review surface: [GitHub PR #16](https://github.com/xuOAO/ugdr/pull/16), targeting `master`.

## Delivered

- Added a public-API integration test that executes the real `ugdr_daemon` and drives two independent Client processes over separate sessions.
- Covered Context, PD, CQ, and RC QP creation; RESET to INIT; out-of-band QPN exchange; bilateral connect; local-only RTS transitions; and child-first cleanup.
- Covered session-disconnect reclamation by exiting one Client without public destroy calls, then proving the other session remains usable, the disconnected QPN returns `ENOENT`, and subsequent QPN allocation does not reuse it.
- Covered invalid-query output atomicity and verified that `ugdr_post_send`, `ugdr_post_recv`, and `ugdr_poll_cq` remain unsupported without modifying caller outputs.
- Registered `ugdr_control_plane_acceptance` with the real daemon executable and a bounded test timeout.
- The acceptance test exposed no reviewed F03 API or control-service semantic drift, so no production implementation changes were required.

## Verification

Executed on 2026-07-21 in `/home/wx/ugdr`:

- `tools/ugdr doctor --json` — exit `0`; all `10/10` checks passed with CUDA `12.3` and one visible NVIDIA GPU.
- `tools/ugdr format --check` — exit `0`; all 45 managed C/C++/CUDA files matched `.clang-format`.
- `tools/ugdr lint` — exit `0`; format, clang-tidy, module boundaries, Client contracts, project docs, skeleton, and project state checks passed.
- `cmake --build build -j2` — exit `0`.
- `ctest --test-dir build -R '^ugdr_control_plane_acceptance$' --repeat until-fail:5 --output-on-failure` — exit `0`; all five real-daemon/two-Client runs passed.
- `ctest --test-dir build -R '^ugdr_cuda_ipc_mr$' --output-on-failure` — exit `0`; the real GPU smoke executed and passed without skip.
- `ctest --test-dir build --output-on-failure` — exit `0`; all `28/28` tests passed in the host environment.
- `python3 tools/check_module_boundaries.py --root . --build-dir build` — exit `0`; seven production targets and fourteen allowed static-link dependency edges passed.
- `python3 tools/check_client_contracts.py --root .`, `python3 tools/check_project_docs.py --root .`, and `python3 tools/project_state.py validate --root .` — exit `0`.
- `git diff --check` — exit `0`.

## Deviations and Remaining

- No reviewed design deviation was introduced.
- F03-S06 adds acceptance coverage only; it does not add a public ABI, control opcode, resource type, data-path capability, or multi-host wire format.
- Real WR processing, completion production, and payload movement remain deferred to later data-path work.
- Human implementation acceptance has not been inferred from tests, commits, or PR state.
