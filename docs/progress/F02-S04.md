# F02-S04 Implementation Record

## Source

- Reviewed feature design: [F02 API 契约与对象模型](../v1_docs/F02_API_契约与对象模型/F02_API_契约与对象模型_功能文档.md), Feishu revision `45`.
- Reviewed step design: [F02-S04 WR/WC 与完成语义契约](../v1_docs/F02_API_契约与对象模型/F02-S04_WR_WC_与完成语义契约_步骤文档.md), Feishu revision `20`, synchronized in `agent` mode with reviewed body hash `77db13e10088957b06eb870c1ff27606967102568a4b2207d09b3504abec1a41`.
- Design handoff commit: `fe21da2` on `codex/f02-s04-wr-wc-semantics`.
- Draft review surface: [GitHub PR #8](https://github.com/xuOAO/ugdr/pull/8), targeting `master`.

## Delivered

- Replaced the opaque MR declaration with the reviewed standard-style public `ugdr_mr` record.
  A future successful registration exposes keys directly as `mr->lkey` and `mr->rkey`; no UGDR
  getter extension was added.
- Added C-compatible `ugdr_sge`, `ugdr_send_wr`, `ugdr_recv_wr`, and `ugdr_wc` definitions with the
  reviewed types, ordering, anonymous immediate-data access, and `wr.wr.rdma` nesting.
- Added the libibverbs-aligned timeout, retry-count, RNR-retry, and minimum-RNR-timer QP attributes
  and mask values. Updated `ugdr_connect_qp` to accept the reviewed const attribute record and mask.
- Added C11 compilation to the build and a dedicated C API fragment. Extended the C++ contract test
  to freeze public signatures, field types and order, enum/mask values, direct MR/WR/WC access, and
  placeholder non-write behavior.
- Added `docs/contracts/wr-wc-semantics.md` for posting and `bad_wr`, Write versus Write With
  Immediate, signaling, RNR, errors, ERR flush, ordering, polling, MR lifetime, and QP destruction.
- Updated the public API, RC state machine, lifecycle, alignment matrix, contract index, and durable
  Decisions 0002/0003. Complete standard records are marked aligned; reviewed projections and
  omitted unsupported union members are explicitly marked subset adaptations.
- Preserved the F02 boundary: no queue, completion, retry engine, daemon object, IPC, network
  transport, or data movement implementation was added. All runtime entry points remain explicit
  `EOPNOTSUPP` placeholders.

## Verification

Executed on 2026-07-21 in `/home/wx/ugdr`:

- `cmake -S . -B build` — exit `0`; C, C++, and CUDA configuration completed.
- `cmake --build build -j2` — exit `0`; the C and C++ API contract fragments, `ugdr_api`, and Client
  compiled and linked.
- `ctest --test-dir build --output-on-failure` — exit `0`; all `12/12` tests passed, including
  `ugdr_api_contract`, repository governance, workflow, and module-boundary fixtures.
- `tools/ugdr lint` — exit `0`; formatting, clang-tidy, module boundaries, project docs, repository
  skeleton, and project state passed (`6/6`).
- `python3 tools/check_module_boundaries.py --root . --build-dir build` — exit `0`; six production
  targets and five allowed dependency edges passed.
- `python3 tools/check_project_docs.py --root .` — exit `0`; reviewed sources, indexes, snapshot
  links, and documentation governance passed.
- `python3 tools/project_state.py validate --root .` — exit `0` before the guarded handoff; the
  F02-S04 `ready_for_implementation` state was structurally valid.
- `git diff --check` — exit `0`; no whitespace errors.
- Primary-header audit against `/usr/include/infiniband/verbs.h` confirmed exact MR fields,
  SGE/Receive WR shapes, relevant Send WR/WC access paths, WC values, and QP mask bits. The omitted
  unsupported opcode/QP-type/invalidated-rkey unions are documented as subset adaptations.

## Deviations and Remaining

- No reviewed design deviation was introduced.
- F02 exposes only linkable placeholders; passing API and documentation contract tests does not
  claim that WR posting, retry, completion, flushing, or data transfer executes at runtime.
- Runtime features must implement the frozen success, failure, ordering, lifetime, RNR, and flush
  behavior without changing the Client-visible surface.
- Send/Recv operations, RDMA Read, Atomic, SRQ, inline, solicited, fence, completion events,
  hardware/network addressing, cross-daemon connection, and serialized connection records remain
  unsupported in v1 unless a later reviewed design explicitly adds them.
- Human implementation acceptance has not been inferred from review approval, tests, commits, or
  PR state. This record intentionally has no Acceptance section before the final human gate.

## Acceptance

- At `2026-07-21T08:48:52+08:00`, the user explicitly confirmed `验收通过` after reviewing the
  F02-S04 implementation and Draft PR #8.
- Feishu revision `21` was read after that confirmation. It contained exactly one checkbox whose
  text was `已实现`, and that checkbox had `done="true"`.
- Acceptance covers the reviewed public MR/SGE/WR/WC records, direct key and RDMA field access,
  retry attribute/mask correction, Write and Write With Immediate semantics, RNR/error/flush rules,
  lifecycle and ordering contracts, placeholder non-write behavior, and recorded verification.
- This acceptance authorizes the guarded transition to `completed`, the final commit and push to
  PR #8, and marking that same PR Ready for review. It does not authorize automatic merge, a direct
  push to `master`, or starting the next scope before merge reconciliation.
