# Progress Records

Store execution and verification records here. These records explain what happened; they are not the machine-readable current state or a design authority.

- Use one Markdown file per feature or step when a durable handoff record is useful.
- Record commands run, results, deviations, and remaining verification.
- Link reviewed design snapshots and relevant decisions.
- Do not infer or record human acceptance unless the user explicitly confirms it.
